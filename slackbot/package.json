{
  "name": "getting-started-typescript",
  "version": "1.0.0",
  "description": "Bolt getting started app in TypeScript",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "startjs": "npm run build && node dist/app.js",
    "start": "ts-node src/app.ts",
    "demo": "ts-node src/demo.ts",
    "bg": "ts-node src/breakglass.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.10.x",
    "dotenv": "^16.0.x",
    "temporalio": "0.19.x"
  },
  "devDependencies": {
    "@types/node": "^17.0.x",
    "ts-node": "^10.7.x",
    "typescript": "^4.6.x"
  }
}
